[project]
name = "ai-tamperguard"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pandas",
  "scikit-learn",
  "jsonschema",
]

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
